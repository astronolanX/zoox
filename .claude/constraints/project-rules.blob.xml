<blob type="constraint" scope="always" updated="2026-01-13" v="2">
  <summary>zoox project constraints</summary>
  <facts>
    <fact>Use uv for package management, not pip</fact>
    <fact>Run tests with: uv run pytest</fact>
    <fact>Local-only package - not published to PyPI</fact>
    <fact>Zero dependencies - stdlib only</fact>
  </facts>
  <context>
    zoox is symbiotic memory for AI - XML polyps that persist across sessions.
    Coral terminology: polyp (blob), reef (glob), current (thread), bedrock (constraint).
    A reef is a collection of polyps in .claude/ directories.
  </context>
</blob>
