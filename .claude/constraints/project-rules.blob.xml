<blob type="constraint" scope="always" updated="2026-01-08" v="2">
  <summary>Goopy project constraints</summary>
  <facts>
    <fact>Use uv for package management, not pip</fact>
    <fact>Run tests with: uv run pytest</fact>
    <fact>Local-only package - not published to PyPI</fact>
    <fact>Zero dependencies - stdlib only</fact>
  </facts>
  <context>
    Goopy is the XML blob system for Claude Code session memory.
    Extracted from flubber to be a standalone, reusable package.
    A "glob" is a collection of blobs in .claude/ directories.
  </context>
</blob>
